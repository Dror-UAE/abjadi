# Wireless (no USB): Mac LAN IP — phone must be on same Wi‑Fi
EXPO_PUBLIC_API_URL=http://192.168.70.212:3001

# You can also change the server URL inside the app after a connection error (no rebuild needed).

# USB dev only:
# EXPO_PUBLIC_API_URL=http://127.0.0.1:3001
# adb reverse tcp:3001 tcp:3001
