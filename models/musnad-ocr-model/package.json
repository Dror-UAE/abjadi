{
  "name": "@abjadi/musnad-ocr-model",
  "version": "0.5.0",
  "private": true,
  "description": "Musnad OCR production inference package (Python)",
  "scripts": {
    "setup:venv": "python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt",
    "predict": ".venv/bin/python predict_cli.py",
    "clean:venv": "rm -rf .venv .turbo inference/__pycache__ outputs"
  }
}
